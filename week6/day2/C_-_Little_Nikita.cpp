#include<bits/stdc++.h>
#define ariful ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
#define lb lower_bound
#define ub upper_bound
#define ll long long
#define vi vector<long long>
#define sot(x) (x).begin(),(x).end()
#define yes cout<<"Yes\n"
#define no cout<<"No\n"
#define line "\n"
#define shesh return 0;
using namespace std;
int main()
{
   ariful
   ll t,m,n,c=0,x,y,z,a,b,d=0;
   cin>>t;
   while(t--)
   {
    cin>>n>>m;
    if(n>=m && (n % 2 == m % 2))
    {
      yes;
    }
    else{
      no;
    }
   }
  shesh
}